machine:
  node:
    version: 4.2.2
deployment:
  production:
    branch: master
    commands:
      - git checkout gh-pages
      - git merge master
      - git push origin gh-pages
      - ./scripts/production.sh
